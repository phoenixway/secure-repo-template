!*.md.age

# Розшифровані файли Markdown (крім README.md)
*.md
!README.md

# --- Секрети та локальна конфігурація ---

# Приватний ключ age (нешифрований) - НІКОЛИ НЕ КОМІТИТИ!
# Замість нього використовується зашифрований варіант (напр. age-key.txt.gpg),
# шлях до якого вказаний у .env (MASTER_AGE_KEY_STORAGE_PATH).
# Цей файл .gitignore має запобігти випадковому коміту нешифрованого ключа,
# якщо він тимчасово з'являється в корені репозиторію.
age-key.txt
# Файл конфігурації середовища - НІКОЛИ НЕ КОМІТИТИ!
# .env.example можна комітити як шаблон.
.env
.env.*
!.env.example

# --- Директорії для генерованого контенту та тимчасових файлів ---

# Директорія для локальних архівів бекапів (самі архіви зашифровані)
# Можна комітити, якщо архіви невеликі і ти хочеш мати їх історію в Git,
# АЛЕ зазвичай бекапи краще тримати окремо або в хмарі.
backup/*
!backup/.gitignore
!backup/.gitkeep


# Директорія для персональних скриптів користувача (не частина шаблону)
personal-scripts/*
!personal-scripts/.gitignore
!personal-scripts/.gitkeep


# Тимчасові файли/директорії від скриптів
tmp-restore-*/
tmp-restore*/
temp_age_key.* # Для тимчасових розшифрованих ключів

# --- Залежності та файли збірки (якщо будуть) ---
# node_modules/
# vendor/
# *.pyc
# __pycache__/

# --- ОС специфічні файли ---
.DS_Store
Thumbs.db

# Python
__pycache__/
*.pyc
venv/
.pytest_cache/

# --- Secrets Directory ---
# Ignore all content inside the secrets directory, but keep the directory itself.
secrets/*
!secrets/.gitkeep