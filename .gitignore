# Python
__pycache__/
*.pyc
venv/
.pytest_cache/

# Розшифровані файли Markdown
vault/*.md

# --- Папка конфігурації ---
# Ігноруємо .env та вміст keys/, але відстежуємо .env.example та порожні папки
config/.env
config/keys/*
!config/keys/.gitkeep

# --- Секрети та локальна конфігурація ---
# Правило для старого age-key.txt, якщо він залишився
age-key.txt

# --- Директорії для генерованого контенту та тимчасових файлів ---
backup/*
!backup/.gitkeep
personal-scripts/*
!personal-scripts/.gitkeep
tmp-restore-*/
temp_age_key.*